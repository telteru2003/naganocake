<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>test</title>
  <style>
    .circle-btn {
        background-color: white;
        border: solid 3px white;
        width: 30px;
        height: 30px;
        border-radius: 50%;
 	      box-shadow: 0 2px 3px rgba(0, 1, 1, 1);
        text-align: center;
        padding: 5px;
        display: inline-block;
        border: 2px solid #ccc;
        line-height: 15px;
        margin: 0 auto;
        text-decoration: none;
        display: flex;
        justify-content: center;
    }
  </style>
</head>

<% provide(:title, "商品") %>
<div class="container">
    <div class="row">
        <div class="col-sm-8 px-sm-0 offset-sm-1">
            <h2 class="my-5">商品一覧</h2>
        </div>
        <div class="col-sm-2 px-sm-0 text-right mt-5">
            <%= link_to new_admin_item_path, class: "btn btn-outline-secondary circle-btn" do %>
            ＋
            <% end %>
        </div>
    </div>
    <div class="row">
    　<div class="col-sm-10 offset-1">
        <%= render 'index', items: @items%>
          <%= paginate @items %>
        </div>
    </div>
</div>