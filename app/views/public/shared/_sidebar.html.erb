
    <table class="ec-genre-list" >
      <tr>
        <td>
           ジャンル検索
        </td>
      </tr>

      <% genres.each do |genre| %>
        <tr>
          <td>
            <%= link_to genre.name, items_path(genre.id) %>
          </td>
        </tr>
      <% end %>

    </table>